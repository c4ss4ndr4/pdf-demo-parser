# PDF Parser Configuration

# Directory paths (relative to project root)
INPUT_DIR=./input
OUTPUT_DIR=./output

# Processing options
LOG_LEVEL=info
BATCH_SIZE=10

# Optional: Custom file patterns (comma-separated extensions)
SUPPORTED_EXTENSIONS=.pdf

# Example: Rate limiting (if needed)
PROCESS_DELAY_MS=0
